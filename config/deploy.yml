# Name of this app
service: fizzy
image: fizzy


#-- About your deployment --#

# Where to deploy fizzy
servers:
  web:
    hosts:
      - fizzy.localdomain  # Set your server name here
    proxy: false
    options:
      publish:
        - "3000:3000"

# How you connect to your server
ssh:
  user: ubuntu  # If you use a different username to SSH to your server, specify it here
  keys: ["/home/elvin/Work/home-lab/.ssh/vm-admin/key"]

# Your application configuration (secrets come from .kamal/secrets).
env:
  secret:
    - SECRET_KEY_BASE
    - VAPID_PUBLIC_KEY
    - VAPID_PRIVATE_KEY
  clear:
    BASE_URL: http://fizzy.hl.elvinefendi.com # The public URL of your Fizzy instance
    MAILER_FROM_ADDRESS: Fizzy <noreply@fizzy.hl.elvinefendi.com> # The email "from" address that Fizzy sends email from
    SMTP_ADDRESS: 192.168.1.207 # aiosmtpd on the VM
    SMTP_PORT: 1025
    MULTI_TENANT: false # Set to true to allow multiple accounts to sign up
    SOLID_QUEUE_IN_PUMA: true # Run background jobs in the app container
    DISABLE_SSL: true


#-- General configuration --#

# Use a local registry to deploy
registry:
  server: localhost:5555

# Handy aliases for interacting with your deployment. For eaxmple: `bin/kamal console` will connect to a
# Rails console in production.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

# Use a persistent storage volume for sqlite database files and local Active Storage files.
# Recommended to change this to a mounted volume path that is backed up off server.
volumes:
  - "fizzy_storage:/rails/storage"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Configure the image builder.
builder:
  arch: amd64
